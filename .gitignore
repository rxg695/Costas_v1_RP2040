# Build directories (keep source tree clean)
build/
build_validation/

# Local cache / tooling scratch
.cache/

# CMake/Ninja generated files (if created in source root)
CMakeCache.txt
cmake_install.cmake
build.ninja
compile_commands.json

# Pico build artifacts
*.elf
*.uf2
*.bin
*.hex
*.map
*.dis

# Generic compiled objects
*.o
*.obj
*.a
*.so

# Editor/OS noise
*~
.DS_Store
Thumbs.db

# Keep project-specific VS Code workspace configuration tracked
!.vscode/
!.vscode/*
